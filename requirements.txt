streamlit==1.35.0

streamlit-autorefresh>=1.0  # (선택) 타이머 자동 갱신용

# (권장) 가상환경
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

# 패키지 설치
pip install -r requirements.txt

# 실행
streamlit run app.py
